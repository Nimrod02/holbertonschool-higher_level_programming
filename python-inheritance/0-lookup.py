#!/usr/bin/python3
"""
module 0-lookup
"""


def lookup(obj):
    """lookup

    Args:
        obj (str): contains all string for the object

    Returns:
        dir(obj): return all the possible string in the object
    """
    return dir(obj)
